---
// 列表页、关于页、搜索页的正文容器。标题不在这里出，列表页靠时间线本身表意。
type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---

<main id="main-content" class:list={["app-layout pb-4", className]}>
  <slot />
</main>
